1. usePathname
=> import {usePathname} from 'next/navigation'
=> 현재 라우트의 경로를 반환합니다. 
=> 클라이언트 컴포넌트에서만 사용이 가능합니다.
=> 쿼리 문자열만 변경될 경우 usePathname은 그대로입니다.


2. 사용법
=> const pathname =  usePathname()
=> pathname(현재 활성화된 경로가 문자열로 반환됩니다.)
