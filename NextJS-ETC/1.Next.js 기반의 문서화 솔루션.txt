1. nextra, next-theme-docs
=> 문서화 사이트를 쉽게 만들 수 있도록 도와주는 라이브러리
=> mdx, md 파일이 아니라면 테마가 설정되지 않는다.
=> mdx는 react 문법과 같이 사용할 수 있는 markdown 언어이다.


2. nextra : Next.js와 함께 사용할 수 있는 정적 사이트 생성기(SSG)

2-1. nextra 설정
=> const withNextra = require('nextra')({
    theme : 'nextra-theme-docs',
    themeConfig : './theme.config.js',
});
module.exports = withNextra({...});