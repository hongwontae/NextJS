1. metadata
1-1. export const metadata = {
    title : ...,
    description : ....,
    ...
} => 이런 형식으로 metadata를 추가하여 엔진 크롤러에 노출시킬 수 있습니다.


2. page.js에 metadata를 저장하면 해당 page.js에서만 적용됩니다.

3. laypout.js에 metadata를 저장하면 중첩된 page.js 모두 적용됩니다.

4. page.js 근처에 있는 metadata가 우선 적용됩니다.


5. 동적 메타 데이터
5-1. export async function generateMetadate({params}){
    return {
        ...
    }
}
5-2. 해당 함수의 이름이 정확해야지만 Next.JS가 대신 실행해줍니다.
5-3. params를 받아서 동적으로 처리할 수 있습니다.